import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Providers } from "@/components/providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Movie Explorer - Discover Your Next Favorite Film",
  description: "Browse, search, and save your favorite movies with Movie Explorer. Discover new films and manage your personal watchlist.",
  keywords: ["movies", "film", "cinema", "TMDB", "watchlist", "favorites"],
  authors: [{ name: "Movie Explorer Team" }],
  openGraph: {
    title: "Movie Explorer",
    description: "Discover your next favorite film",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
